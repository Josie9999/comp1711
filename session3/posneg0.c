#include <stdio.h>
int main(){
    int num = 0;
    if (num > 0){
        printf("%d is a positive number\n", num);
    }
    else if (num = 0){
        printf("%d is equal to zero\n", num); 
    }
    else{
        printf("%d is a negative number\n", num);
    }
    return 0;
}